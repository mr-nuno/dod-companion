# Vite dev-only config (loaded automatically by `npm run dev`).
# The BFF base URL the SPA calls during local development.
# In Docker/production this is ignored — the API serves the SPA from the same origin.
VITE_API_BASE_URL=http://localhost:5116
